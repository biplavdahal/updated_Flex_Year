import 'package:flutter/material.dart';

class ExitSurveyItem extends StatelessWidget {
  const ExitSurveyItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
