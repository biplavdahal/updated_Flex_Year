/// Determines for how long a snackbar should show.
/// short = 1 seconds and long = 3 seconds.
enum ESnackbarDuration { long, short }

/// A struct that contains the type of snackbar.
enum ESnackbarType { info, error, warning, success }
